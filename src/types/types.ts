export interface projectContent {
    projectName: string,
    description: string,
    url: string,
}

export interface albumContent {
    imageURL: string,
    albumName: string,
    albumArtist: string,
    url: string,
}